;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 함부로 지우지 말아 주세요
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/01
;**********************************************************


;**********************************************************
;성격으로 은어를 바꾸기 위한 템플렛
;**********************************************************
;eXample(예)의 은어
@SLANG_XXXX(ARG)



IF TALENT:임신
	SLANGWORD_XXXX+="임신"
ENDIF
IF TALENT:기생
	SLANGWORD_XXXX+="기생"
ENDIF


;**********************************************************
;외관으로 은어를 바꾸기 위한 템플렛
;**********************************************************
IF TALENT:백보지 > 0
	SLANGWORD_XXXX+="백보지"
ENDIF
IF TALENT:
	SLANGWORD_XXXX+=""
ENDIF





;**********************************************************
;체격・외관으로 은어를 바꾸기 위한 템플렛
;**********************************************************
IF TALENT:왜소함
	SLANGWORD_XXXX+="왜소함"
ENDIF
;원문은 大柄
IF TALENT:거인
	SLANGWORD_XXXX+=""
ENDIF

IF TALENT:외관
	SLANGWORD_XXXX+=""
ENDIF
IF TALENT:변신시외관
	SLANGWORD_XXXX+=""
ENDIF

;**********************************************************
;가슴으로 은어를 바꾸기 위한 템플렛
;**********************************************************

IF TALENT:거유 > 0
	IF TALENT:거유 == 5
		SLANGWORD_XXXX+="기유"
	ELSEIF TALENT:거유 == 4
		SLANGWORD_XXXX+="마유"
	ELSEIF TALENT:거유 == 3
		SLANGWORD_XXXX+="초유"
	ELSEIF TALENT:거유 == 2
		SLANGWORD_XXXX+="폭유"
	ELSE
		SLANGWORD_XXXX+="거유"
	ENDIF
ELSEIF TALENT:빈유 > 0
	IF TALENT:빈유==2
		SLANGWORD_XXXX+="절벽"
	ELSE
		SLANGWORD_XXXX+="빈유"
	ENDIF
ELSE
	SLANGWORD_XXXX+="병유"
ENDIF



