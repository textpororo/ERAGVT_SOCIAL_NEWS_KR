;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は勝手に消さないでください
;書き込む順番は、クドくなり過ぎないように3要素と部位で表現します
;【実況】汁に濡れている、기생されている、촉수 구속구などを装着など
;【形状】大きさ、深さなど、
;【動作】開閉している、うごめいているなど、動きを表現
;【部位呼称】음란や발정や性格で下品な書き方に
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/01
;**********************************************************


;**********************************************************
;性格でスラングを切り替えるためテンプレ
;**********************************************************
;eXample(例)の淫語
@SLANG_XXXX(ARG)



IF TALENT:임신
	SLANGWORD_XXXX+="임신"
ENDIF
IF TALENT:기생
	SLANGWORD_XXXX+="기생"
ENDIF


;**********************************************************
;외관でスラングを切り替えるためテンプレ
;**********************************************************
IF TALENT:백보지 > 0
	SLANGWORD_XXXX+="백보지"
ENDIF
IF TALENT:
	SLANGWORD_XXXX+=""
ENDIF





;**********************************************************
;体格・외관でスラングを切り替えるためテンプレ
;**********************************************************
IF TALENT:왜소함
	SLANGWORD_XXXX+="왜소함"
ENDIF
IF TALENT:大柄
	SLANGWORD_XXXX+="大柄"
ENDIF

IF TALENT:외관
	SLANGWORD_XXXX+=""
ENDIF
IF TALENT:변신시외관
	SLANGWORD_XXXX+=""
ENDIF

;**********************************************************
;おっぱいでスラングを切り替えるためテンプレ
;**********************************************************

IF TALENT:거유 > 0
	IF TALENT:거유 == 5
		SLANGWORD_XXXX+="기유"
	ELSEIF TALENT:거유 == 4
		SLANGWORD_XXXX+="마유"
	ELSEIF TALENT:거유 == 3
		SLANGWORD_XXXX+="초유"
	ELSEIF TALENT:거유 == 2
		SLANGWORD_XXXX+="폭유"
	ELSE
		SLANGWORD_XXXX+="거유"
	ENDIF
ELSEIF TALENT:빈유 > 0
	IF TALENT:빈유==2
		SLANGWORD_XXXX+="絶壁"
	ELSE
		SLANGWORD_XXXX+="빈유"
	ENDIF
ELSE
	SLANGWORD_XXXX+="並乳"
ENDIF



