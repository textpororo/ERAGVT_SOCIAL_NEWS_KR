;**********************************************************
;변수ARG의 ARG:1번째 자릿수의 값을 반환하는 식중 함수
;일단 18번째 자릿수까지 취득 가능
@FIGURE_SPLIT(ARG,ARG:1)
#FUNCTION
IF ARG:1 >= 2
	FOR LOCAL, 0,(ARG:1 - 1)
		ARG = ARG / 10
	NEXT
ENDIF
LOCAL = ARG % 10
RETURNF LOCAL