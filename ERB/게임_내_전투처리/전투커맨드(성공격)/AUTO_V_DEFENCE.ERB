;청순파による自動Ｖ방어
@AUTO_V_DEFENCE, ARG
SIF TFLAG:81 > 0
	RETURN 1
SIF BASE:ARG:Ｖ결계내구력 > 0
	RETURN 1
IF TCVARn:12 & 기절 || (TCVARn:41 == -1 && CFLAG:(CFLAG:1==0 ? 40 # 41) == 199)
	TFLAG:81 = 1
	RETURN 1
ENDIF
IF TALENT:ARG:처녀 > 0
	PRINTL このままだと처녀を奪われそうだ……
ELSE
	PRINTL このままだと前の穴に挿入されそうだ……
ENDIF
PRINTL 咄嗟にＶ삽입방어しますか？
PRINTL [0]する
PRINTL [1]しない
$INPUT_LOOP
INPUT
IF RESULT == 0
	TCVARn:2 = 자세：Ｖ방어
	TFLAG:81 = 1
ELSEIF RESULT == 1
	TFLAG:81 = 1
ELSE
	GOTO INPUT_LOOP
ENDIF
PRINTL 
RETURN RESULT

