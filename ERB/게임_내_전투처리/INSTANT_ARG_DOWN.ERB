@INSTANT_ARG_DOWN
;미변신 상태라면 능력 저하 5회
IF CFLAG:1 == 0
	LOCAL:1 = 5
	PRINTFORMW 적을 둘러싼 독기가 몸에 스며든다……
;변신・SP변신 상태라면 능력 저하 2회
ELSE
	LOCAL:1 = 2
	PRINTFORMW 적을 둘러싼 독기가 주위에 퍼져 있다……
ENDIF
$LOOP_I_ARG_DOWN
;공격・방어・민첩・지성・기초 체력(10) 중 하나를 줄인다
LOCAL:2 = RAND:5
IF LOCAL:2 == 0
	BASE:TARGET:공격 -= 1
ELSEIF LOCAL:2 == 1
	BASE:TARGET:방어 -= 1
ELSEIF LOCAL:2 == 2
	BASE:TARGET:민첩 -= 1
ELSEIF LOCAL:2 == 3
	BASE:TARGET:지성 -= 1
ELSE
	BASE:TARGET:기초체력 -= 10
ENDIF
;LOCAL:1이 0이 될 때까지 반복한다
LOCAL:1 -= 1
IF LOCAL:1 > 0
	GOTO LOOP_I_ARG_DOWN
ENDIF
;존나 불친절하게 이런 중요한 정보를 안 알려주니까 직접 썼음
PRINTFORMW %PRINT_TRANSCALLNAME(TARGET)%의 기초 능력이 일부 감소하였다！

