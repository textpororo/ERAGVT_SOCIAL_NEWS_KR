
;--------------------------------
;최종 보스 강화, 최대 HP가 ５배로 증가한다
;--------------------------------
@LASTBOSS_KYOUKA_F
#FUNCTION
IF CONFIG_CHECK_BALANCE_F(5) > 0 && ENEMY_TYPE_CHECK_F("LASTBOSS") >= 1
	RETURNF 5
ELSE
	RETURNF 1
ENDIF

;--------------------------------
;최종 보스 강화 시, 회복량을 통상 레벨로 떨어뜨린다
;--------------------------------
@LASTBOSS_REST
IF CONFIG_CHECK_BALANCE_F(5) > 0 && ENEMY_TYPE_CHECK_F("LASTBOSS") >= 1
	LOCAL = 8
ELSE
	LOCAL = 0
ENDIF
RETURN LOCAL
