;쥐어짠다
@COM104

;거리 표시
CALL PRINT_DISTANCE
PRINTL 

;방심도 상승
TFLAG:3 += 80 + 20 * ABL:기교

;방어 플래그：쥐어짠다로 한다
TCVARn:2 = 자세：쥐어짠다

;지문: 쥐어짠다
CALL MESSAGE_BATTLE_CHARA_SEX_ATTACK104
;지문: 세뇌/흑화 캐릭터가 조작 캐릭터의「쥐어짠다」를 봤다
SIF ENEMY_TYPE_CHECK_F("AKUOTI") == 1
	CALL MESSAGE_OTHER_BATTLE_CHARA_SEX_ATTACK104

;사정 게이지를 기록
LOCAL:1 = FLAG:15

;사정
LOCAL = 225 + ABL:기교 * ABL:기교 * SQRT(FLAG:15 / 2 + 50)
CALL TENTACLE_SYASEI_UP, LOCAL

PRINTL
IF (FLAG:110 == 0)
	CALL TENTACLE_ACCESS, "NAME"
ELSEIF (FLAG:110 == 1)
	PRINTFORM %PRINT_TRANSCALLNAME(FLAG:111)%
ENDIF
PRINT 에게 
FONTBOLD
PRINTFORM {FLAG:15 - LOCAL:1} 의 쾌락
FONTREGULAR
PRINTL 을 줬다！
PRINTW 

;촉수의 사정 체크
CALL TENTACLE_SYASEI_CHECK
PRINTL 

;습득 획득
COMMON_PALAM:6 += 450 + 25 * (ABL:순종 + ABL:봉사정신 * 2)

;행동 포인트를 +1
EX:행동포인트 += 1

RETURN 1
