;가만히 있는다
@COM11

;거리 표시
CALL PRINT_DISTANCE
PRINTL 

;방어 플래그：가만히있는다로 한다
TCVARn:2 = 자세：가만히있는다

;방심도 상승
TFLAG:3 += 25

;기절해 있는 경우
IF (TCVARn:12 & 기절)
	PRINTFORML %조사처리(PRINT_TRANSCALLNAME(TARGET),"는")% 의식을 잃은 상태이다……
	PRINTW 
ELSE
	;지문: 가만히 있는다
	CALL MESSAGE_BATTLE_CHARA_NASUGAMAMA
	;지문: 세뇌/흑화 캐릭터가 조작 캐릭터의「가만히있는다」를 봤다
	SIF ENEMY_TYPE_CHECK_F("AKUOTI") == 1
		CALL MESSAGE_OTHER_BATTLE_CHARA_NASUGAMAMA
ENDIF

RETURN 1
