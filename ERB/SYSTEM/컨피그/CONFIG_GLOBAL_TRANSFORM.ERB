;GLOBAL　변신 처리 선택
;특별 활동 등을 실행할 때의 자동 변신 설정
;비트 연산이 아닌 대입이라는 점에 주의
;특별 활동
;GLOBAL:51 통상 변신일 경우
;GLOBAL:52 여체화 변신일 경우
;GLOBAL:53 남성화 변신일 경우
;정보 수집
;GLOBAL:54 통상 변신일 경우
;GLOBAL:55 여체화 변신일 경우
;GLOBAL:56 남성화 변신일 경우
;여가 활동 (息抜き)
;GLOBAL:57 통상 변신일 경우
;GLOBAL:58 여체화 변신일 경우
;GLOBAL:59 남성화 변신일 경우
;0 항상 선택한다
;1 통상, 변신한다
;2 통상, 변신하지 않는다
;---------------------------------------------
;필터 처리
;---------------------------------------------
@CONFIG_T
#DIM LCOUNT
#DIM OPTION,10
LOCAL = LINECOUNT
FOR LCOUNT, 1, 10
	OPTION:LCOUNT = GLOBAL:(LCOUNT + 50)
NEXT
$MASTER_LOOP
PRINTL 
DRAWLINE
PRINTL 여기서 변신 설정을 지정해 두면, 이후 변신 여부에 대한 선택지를 생략할 수 있습니다.
PRINTL 
PRINTL 특별 활동 시의 변신 설정
PRINTL
PRINT 　[11] 통상 변신의 경우　　　　　     【
IF OPTION:1 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:1 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[12] 변신 시 여체화하는 경우        【
IF OPTION:2 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:2 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[13] 변신 시 남성화하는 경우        【
IF OPTION:3 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:3 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINTL 
PRINTL 정보 수집 시의 변신 설정
PRINTL
PRINT 　[14] 통상 변신의 경우　　　　　     【
IF OPTION:4 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:4 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[15] 변신 시 여체화하는 경우        【
IF OPTION:5 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:5 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[16] 변신 시 남성화하는 경우        【
IF OPTION:6 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:6 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINTL 
PRINTL 여가 활동 시의 설정
PRINTL
PRINT 　[17] 통상 변신의 경우　　　　　     【
IF OPTION:7 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:7 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[18] 변신 시 여체화하는 경우        【
IF OPTION:8 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:8 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINT 　[19] 변신 시 남성화하는 경우        【
IF OPTION:9 == 1
	PRINT 항상 변신한다
ELSEIF OPTION:9 == 2
	PRINT 항상 변신하지 않는다
ELSE
	PRINT 선택지를 표시한다
ENDIF
PRINTL 】
PRINTL 
PRINTL [200] 변경 사항을 저장하고 돌아간다
PRINTL 
$INPUT_LOOP
INPUT
IF RESULT > 10 && RESULT < 20
	IF OPTION:(RESULT - 10) == 1
		OPTION:(RESULT - 10) = 2
	ELSEIF OPTION:(RESULT - 10) == 2
		OPTION:(RESULT - 10) = 0
	ELSE
		OPTION:(RESULT - 10) = 1
	ENDIF
	CLEARLINE LINECOUNT - LOCAL
	GOTO MASTER_LOOP
ELSEIF RESULT == 200
	FOR LCOUNT, 1, 10
		GLOBAL:(LCOUNT + 50) = OPTION:LCOUNT
	NEXT
	SAVEGLOBAL
	RETURN
ELSE
	GOTO INPUT_LOOP
ENDIF



