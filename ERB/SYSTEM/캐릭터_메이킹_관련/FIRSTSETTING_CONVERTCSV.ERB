;**********************************************************
;CSV의 RELATION을 독자 사양에 맞춰 컨버트
@CONVERT_RELATION
#DIM CHARA
#DIM SELECT
#DIM RELATION_LIST, CHARACSV최대수
;모든 캐릭터분 루프 (全キャラ分ループ)
FOR CHARA, 0, CHARANUM
	SIF CHARA == MASTER
		CONTINUE
	;모든 RELATION의 컨버트
	;CSV의 값을 리스트에 복사
	FOR SELECT, 0, CHARACSV최대수
		RELATION_LIST:SELECT = RELATION:CHARA:SELECT
	NEXT
	;리스트에 해당하는 캐릭터가 존재한다면 그 위치에 또 복사
	FOR SELECT, 0, CHARANUM
		SIF SELECT == MASTER
			CONTINUE
		SIF RELATION_LIST:(NO:SELECT) > 0
			RELATION:CHARA:SELECT = RELATION_LIST:(NO:SELECT)
	NEXT
NEXT
;컨버트 종료









