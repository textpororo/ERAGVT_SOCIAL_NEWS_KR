;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;대음순 %SLANG_LabiaMajora(TARGET)% 과
;소음순 %SLANG_LabiaMinora(TARGET)% 으로 더욱 세분화되어있다
;
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Labia(陰唇)의 음어(淫語)
@SLANG_Labia(ARG)
#DIM LCOUNT
#DIMS SLANGWORD_Labia
#FUNCTIONS

;초기화
SLANGWORD_Labia =


;1/3 확률로 대음순만, 1/3 확률로 소음순만, 1/3 확률로 대음순・소음순 양쪽을
LCOUNT = RAND:99
IF LCOUNT > 65
	;여성기를 숨기는 피하지방이 많은 부분, 바깥쪽의 균열(ワレメ)을 말한다
	SLANGWORD_Labia = %SLANGWORD_Labia% %SLANG_LabiaMajora(TARGET)%
ELSEIF LCOUNT > 32
	;여성기의 질 구멍을 가리는 부분, 안쪽의 비라비라(ビラビラ)를 말한다
	SLANGWORD_Labia = %SLANGWORD_Labia% %SLANG_LabiaMinora(TARGET)%
ELSE
	;여성기를 숨기는 피하지방이 많은 부분, 바깥쪽의 균열(ワレメ)을 말한다
	SLANGWORD_Labia = %SLANGWORD_Labia% %SLANG_LabiaMajora(TARGET)%

	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_Labia += 조사만처리(SLANG_LabiaMajora(TARGET),"로") + " 둘러싸인"
			;SLANGWORD_Labia += "に囲まれた"
		CASEELSE
			SLANGWORD_Labia += "의 안쪽에"
	ENDSELECT

	;여성기의 질 구멍을 가리는 부분, 안쪽의 비라비라(ビラビラ)를 말한다
	SLANGWORD_Labia = %SLANGWORD_Labia% %SLANG_LabiaMinora(TARGET)%
ENDIF


;합성하여 완성된 음어를 반환한다
RETURNF SLANGWORD_Labia




