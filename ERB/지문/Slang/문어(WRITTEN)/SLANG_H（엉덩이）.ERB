;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;엉덩이 구멍(尻穴) 이외의 엉덩이살(尻肉)을 표현한다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Hip(尻)의 음어(淫語)
;**********************************************************
@SLANG_H(ARG)
#DIMS SLANGWORD_H
#FUNCTIONS

;초기화
SLANGWORD_H =

;제1형용사：상황（즙에 젖어있는 등등）
SIF (STAIN:4 & 4)
	SLANGWORD_H = %SLANGWORD_H% %조사처리(SLANG_SEMEN(TARGET),"가")% 뿌려져 있는 

;제2형용사：형태 표현
;순산형
;원문은 どっしりとしている
IF TALENT:812 == 1
	SLANGWORD_H += "풍만한"
;포동포동
ELSEIF TALENT:812 == 2
	SLANGWORD_H += "포동포동한"
;유아배
;원문은 ぷりぷりしている
ELSEIF TALENT:812 == 3
	SLANGWORD_H += "탱탱한"
;날씬함
;원문은 肉付きの薄い
ELSEIF TALENT:812 == 4
	SLANGWORD_H += "살집이 얇은"
;빵뎅이
;원문은 むっちりしている
ELSEIF TALENT:812 == 5
	SLANGWORD_H += "빵빵한"
;왕뎅이
;원문은 むにゅっとした肉付きの
ELSEIF TALENT:812 == 6
	SLANGWORD_H += "몰캉몰캉한 살집의"
ENDIF

;제2형용사 뒤에 띄어쓰기 추가
SIF TALENT:812 > 0
	SLANGWORD_H += " "

;제3형용사：동작 표현


;부위 호칭
IF TALENT:음고>0
	SLANGWORD_H += "음고"
ELSEIF TALENT:음란>0
	SELECTCASE RAND:3
		CASE 2
			SLANGWORD_H += "애플힙"
		CASE 1
			SLANGWORD_H += "피치힙"
		CASEELSE
			SLANGWORD_H += "궁둥이"
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 3
			SLANGWORD_H += "엉덩이 골짜기"
		CASE 2
			SLANGWORD_H += "힙"
		CASE 1
			SLANGWORD_H += "엉덩이 살"
		CASEELSE
			SLANGWORD_H += "엉덩이"
	ENDSELECT
ENDIF

RETURNF SLANGWORD_H


;*************【음어 메모（미등록）】***********************
;빵뎅이
;통통한
;
;
;
;
;
;*************【음어 메모（미등록）】***********************



