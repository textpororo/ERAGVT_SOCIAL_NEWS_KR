;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;Bust(가슴)에 대해서는, @SLANG_B 를 참고
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Nipple(ニップル)의 음어(淫語)
;**********************************************************
@SLANG_N(ARG)
#DIMS SLANGWORD_N
#FUNCTIONS

;초기화
SLANGWORD_N=

;제1형용사：상황（즙에 젖어있는 등등）
SIF (STAIN:2 & 4)
	SLANGWORD_N += "정액이 발라진 "

;제2형용사：형태 표현


;제3형용사：동작 표현
;원문은 快楽にそそり勃つ
SIF TALENT:음유
	SLANGWORD_N += "쾌락에 솟아 오른 "

;부위 호칭
;원문은 乳芽 유아 인데, 그대로 쓰면 못 알아볼 것 같아서 바꿨다
;그런 김에 몇개 추가해 봤는데, 남자일 경우는 생각 안 해봤다 ㅁ?ㄹ
IF TALENT:음유
	SELECTCASE RAND:3
		CASE 2
			SLANGWORD_N += "젖가슴 돌기"
		CASE 1
			SLANGWORD_N += "음탕한 꼭지"
		CASEELSE
			SLANGWORD_N += "젖 손잡이"
	ENDSELECT
ELSE
	SELECTCASE RAND:5
		CASE 4
			SLANGWORD_N += "유두"
		CASE 3
			SLANGWORD_N += "가슴 끝부분"
		;원문은 乳先
		CASE 2
			SLANGWORD_N += "가슴 끝"
		CASE 1
			SLANGWORD_N += "젖꼭지"
		CASEELSE
			SLANGWORD_N += "니플"
	ENDSELECT
ENDIF

RETURNF SLANGWORD_N


;*************【음어 메모（미등록）】***********************
;
;
;*************【음어 메모（미등록）】***********************



