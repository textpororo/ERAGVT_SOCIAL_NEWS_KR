;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;Clitoris(클리토리스)의 표현은 @SLANG_C 로
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Penis(ペニス)의 음어(淫語)
;**********************************************************
@SLANG_P(ARG)
#FUNCTIONS
#DIMS SLANGWORD_P

;초기화
SLANGWORD_P =


;제1형용사：상황（즙에 젖어있는 등등）
IF TALENT:백보지 == 1
	SELECTCASE RAND:3
		CASE 2
		SLANGWORD_P += "털 없는"

		CASE 1
		SLANGWORD_P += "매끈매끈한"

		CASEELSE
		SLANGWORD_P += "털도 나지 않은"

	ENDSELECT

	;제1형용사 뒤에 띄어쓰기 추가
	SLANGWORD_P += " "
ENDIF

SELECTCASE TALENT:후타나리
	;정착 후타나리
	CASE 4
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		SLANGWORD_P += "정착 후타나리"


	;천연 후타나리
	CASE 3
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		SLANGWORD_P += "천연 후타나리"


	;기생후타나리
	CASE 2
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		SLANGWORD_P += "기생 후타나리"


	;후천 후타나리
	CASE 1
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		SLANGWORD_P += "후천 후타나리"


	;후타나리以外
	CASEELSE
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		;부위 호칭
		IF ISMALE()
			IF TALENT:음핵
				SLANGWORD_P += "음핵"
			ELSE
				SELECTCASE RAND:8
					;역자가 추가해 보고 싶어서 썼다
					CASE 7
						SLANGWORD_P += "육봉"
					;원문은 ちんぽ
					CASE 6
						SLANGWORD_P += "자지"
					CASE 5
						SLANGWORD_P += "음경"
					CASE 4
						SLANGWORD_P += "남성기"
					;원문은 ちんちん
					CASE 3
						SLANGWORD_P += "꼬추"
					CASE 2
						SLANGWORD_P += "남근"
					CASE 1
						SLANGWORD_P += "페니스"
					;원문은 ちんこ
					CASEELSE
						SLANGWORD_P += "쥬지"
				ENDSELECT
			ENDIF
		ENDIF

ENDSELECT







RETURNF SLANGWORD_P

;*************【음어 메모（미등록）】***********************
;
;노장(怒張)
;육돌기(肉突起)
;육산(肉傘)
;기러기 목?(雁首 YM 번역명에서 따옴)
;귀두
;
;*************【음어 메모（미등록）】***********************



