;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;남자나 후타나리의 페니스(남근)에 관해서는, @SLANG_P 로
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Clitoris(클리토리스)의 음어(淫語)
;**********************************************************
@SLANG_C(ARG)
#FUNCTIONS
#DIMS SLANGWORD_C

;초기화
SLANGWORD_C =

;제1형용사：상황（즙에 젖어있는 등등）
;제2형용사：형태 표현
;제3형용사：동작 표현
;부위 호칭
;남자나 후타나리는, 페니스를 호출
IF ISMALE() ||  TALENT:후타나리 > 0
	SLANGWORD_C = %SLANG_P(TARGET)%
ELSE
	IF TALENT:음핵
		SLANGWORD_C += "음핵"
	ELSE
		SELECTCASE RAND:3
			CASE 2
				SLANGWORD_C += "콩알"
			CASE 1
				SLANGWORD_C += "야한 돌기"
			CASEELSE
				SLANGWORD_C += "클리토리스"
		ENDSELECT
	ENDIF
ENDIF
RETURNF SLANGWORD_C

;*************【음어 메모（미등록）】***********************
;
;육두(肉豆)
;육아(肉芽)
;음핵(陰核)
;
;*************【음어 메모（미등록）】***********************



