;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;안쪽의 비라비라(ビラビラ)에 대해서는 쓰지 말아 주세요
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;LabiaMajora(大陰唇)의 음어(淫語)
@SLANG_LabiaMajora(ARG)
#DIMS SLANGWORD_LabiaMajora
#FUNCTIONS

;초기화
SLANGWORD_LabiaMajora =

;제1형용사：상황（즙에 젖어있다 등등）
;STAIN:3: 질, 균열
IF (STAIN:3 & 4)
	SLANGWORD_LabiaMajora = %SLANGWORD_LabiaMajora% %SLANG_SEMEN(TARGET)%
	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_LabiaMajora += 조사만처리(SLANG_SEMEN(TARGET),"로") + " 더럽혀져 있는"
		CASEELSE
			SLANGWORD_LabiaMajora += "에 젖어 뚝뚝 떨어지고 있는"
	ENDSELECT

	;제1형용사 뒤에 띄어쓰기 추가
	SLANGWORD_LabiaMajora += " "
ENDIF



;제2형용사：형태 표현
;순산형
;원문은 プリプリしている
IF TALENT:812 == 1
	SLANGWORD_LabiaMajora += "쭉쭉빵빵한"
;포동포동
;원문은 ムチムチしている
ELSEIF TALENT:812 == 2
	SLANGWORD_LabiaMajora += "포동포동한"
;유아배
;원문은 プニっとしている
ELSEIF TALENT:812 == 3
	SLANGWORD_LabiaMajora += "뽀잉뽀잉한"
;날씬함
;원문은 スラッとしている
ELSEIF TALENT:812 == 4
	SLANGWORD_LabiaMajora += "늘씬한"
;빵뎅이
;원문은 ムチッとしている
ELSEIF TALENT:812 == 5
	SLANGWORD_LabiaMajora += "탱탱한"
;왕뎅이
;원문은 ムニッとしている
ELSEIF TALENT:812 == 6
	SLANGWORD_LabiaMajora += "몰캉몰캉한"
ENDIF

;제2형용사 뒤에 띄어쓰기 추가
SIF TALENT:812 > 0
	SLANGWORD_LabiaMajora += " "

;제3형용사：동작 표현
IF TALENT:음란 > 0
	SELECTCASE RAND:2
		;원문은 期待で開く
		CASE 1
			SLANGWORD_LabiaMajora += "기대하는 듯 벌려진"
		;원문은 興奮で開く
		CASEELSE
			SLANGWORD_LabiaMajora += "흥분으로 벌려진"
	ENDSELECT

	;제3형용사 뒤에 띄어쓰기 추가
	SLANGWORD_LabiaMajora += " "
ENDIF



;부위 호칭
IF TALENT:음란 > 0
	SELECTCASE RAND:3
		CASE 2
			SLANGWORD_LabiaMajora += "음란한 틈새"
		CASE 1
			SLANGWORD_LabiaMajora += "음란한 골짜기"
		CASEELSE
			SLANGWORD_LabiaMajora += "음란한 균열"
	ENDSELECT
ELSE
	SELECTCASE RAND:4
		CASE 3
			SLANGWORD_LabiaMajora += "대음순"
		CASE 2
			SLANGWORD_LabiaMajora += "음렬"
		CASE 1
			SLANGWORD_LabiaMajora += "균열"
		CASEELSE
			SLANGWORD_LabiaMajora += "일자 균열"
	ENDSELECT
ENDIF


;합성하여 완성된 음어를 반환한다
RETURNF SLANGWORD_LabiaMajora





;*************【음어 메모（미등록）】***********************
;미육의 조개(媚肉の二枚貝)
;
;
;
;
;*************【음어 메모（미등록）】***********************





