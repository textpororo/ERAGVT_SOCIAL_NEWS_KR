;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;쓰는 순서는, 너무 장황해지지 않도록 3요소와 부위로 표현합니다
;【상황】즙에 젖어 있는, 기생되어 있는, 촉수 구속구 등을 장착한 등등
;【형태】크기, 깊이 등
;【동작】벌려진, 꿈틀거리는 등등의 움직임을 표현
;【부위 호칭】음란, 발정, 성격으로 저속한 쓰기(書き方)로
;
;질 구멍의 표현이며, Labia(음순)는 @SLANG_Labia 에서 표현한다
;Womb(자궁)의 표현도 여기서는 피한다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Vagina(ヴァギナ)의 음어(淫語)
;**********************************************************
@SLANG_V(ARG)
#DIMS SLANGWORD_V
#FUNCTIONS

;초기화
SLANGWORD_V =

;제1형용사：상황（즙에 젖어있는 등등）
SIF (STAIN:3 & 4)
	SLANGWORD_V = %SLANGWORD_V% %SLANG_SEMEN(TARGET)%에 젖어 뚝뚝 떨어지는 

;제2형용사：형태 표현
;제3형용사：동작 표현


;부위 호칭
IF TALENT:음호
	SLANGWORD_V += "음호"
ELSEIF TALENT:음란
	SELECTCASE RAND:3
		CASE 2
			SLANGWORD_V += "살단지"
		CASE 1
			SLANGWORD_V += "암구멍"
		;원문은 ドスケベマンコ
		CASEELSE
			SLANGWORD_V += "개변태 보지"
	ENDSELECT
ELSE
	SELECTCASE RAND:4
		CASE 3
			SLANGWORD_V += "보지"
		CASE 2
			SLANGWORD_V += "여성기"
		CASE 1
			SLANGWORD_V += "바기나"
		CASEELSE
			SLANGWORD_V += "질"
	ENDSELECT
ENDIF

RETURNF SLANGWORD_V

;*************【음어 메모（미등록）】***********************
;육동(肉洞)
;육벽(肉壁)
;육통(肉筒)
;질통(膣筒)
;산도(産道)
;
;*************【음어 메모（미등록）】***********************



