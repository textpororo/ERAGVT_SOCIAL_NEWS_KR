;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「남근」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;사정치나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;또한, 촉수의 남근이 아닌「캐릭터 측의 가랑이의 남근」에 사용합니다.
;
;자기 자신에게 자라 있는 남근이므로,「자지님(おちんぽ様)」과 같은 굴복한 문장은,
;기생후타나리 이외에는 넣지 맙시다.
;「자지님(おちんぽ様)」이라고 넣는 것은, 남성기형의 촉수 자지 %S_T_SHAPE("","남근")% 등입니다.
;
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Penis(ペニス)의 음어(淫語)
;**********************************************************
@S_SLANG_P(ARG)
#FUNCTIONS
#DIMS S_SLANGWORD_P

;초기화
S_SLANGWORD_P =


;제1형용사：상황（즙에 젖어있는 등등）
IF TALENT:백보지 == 1
	SELECTCASE RAND:3
		CASE 2
		S_SLANGWORD_P += "털 없는"

		CASE 1
		S_SLANGWORD_P += "매끈매끈한"

		CASEELSE
		S_SLANGWORD_P += "털도 나지 않은"
	ENDSELECT
	
	;제1형용사 뒤에 띄어쓰기 추가
	S_SLANGWORD_P += " "
ENDIF

SELECTCASE TALENT:후타나리
	;정착 후타나리
	CASE 4
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		S_SLANGWORD_P += "정착 후타나리"


	;천연 후타나리
	CASE 3
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		S_SLANGWORD_P += "천연 후타나리"


	;기생후타나리
	CASE 2
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		S_SLANGWORD_P += "기생 후타나리"


	;후천 후타나리
	CASE 1
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		S_SLANGWORD_P += "후천 후타나리"


	;후타나리 이외
	CASEELSE
		;제2형용사：형태 표현
		;제3형용사：동작 표현
		;부위 호칭
		IF ISMALE()
			IF TALENT:음핵
				S_SLANGWORD_P += "음핵"
			ELSE
				SELECTCASE RAND:8
					;역자가 추가해 보고 싶어서 썼다
					CASE 7
						S_SLANGWORD_P += "육봉"
					;원문은 ちんぽ
					CASE 6
						S_SLANGWORD_P += "자지"
					CASE 5
						S_SLANGWORD_P += "음경"
					CASE 4
						S_SLANGWORD_P += "남성기"
					;원문은 ちんちん
					CASE 3
						S_SLANGWORD_P += "꼬추"
					CASE 2
						S_SLANGWORD_P += "남근"
					CASE 1
						S_SLANGWORD_P += "페니스"
					;원문은 ちんこ
					CASEELSE
						S_SLANGWORD_P += "쥬지"
				ENDSELECT
			ENDIF
		ENDIF

ENDSELECT







RETURNF S_SLANGWORD_P

;*************【음어 메모（미등록）】***********************
;
;노장(怒張)
;육돌기(肉突起)
;육산(肉傘)
;기러기 목?(雁首 YM 번역명에서 따옴)
;귀두
;
;*************【음어 메모（미등록）】***********************



