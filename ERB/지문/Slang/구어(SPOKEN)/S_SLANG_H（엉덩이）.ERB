;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「엉덩이」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;엉덩이 구멍(尻穴) 이외의 엉덩이살(尻肉)을 표현한다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Hip(尻)의 음어(淫語)
;**********************************************************
@S_SLANG_H(ARG)
#DIMS S_SLANGWORD_H
#FUNCTIONS

;초기화
S_SLANGWORD_H =


;부위 호칭
IF TALENT:음고>0
	S_SLANGWORD_H += "음고"
ELSEIF TALENT:음란 > 0
	SELECTCASE RAND:3
		CASE 2
			S_SLANGWORD_H += "애플힙"
		CASE 1
			S_SLANGWORD_H += "피치힙"
		;원문은 尻鞠
		CASEELSE
			S_SLANGWORD_H += "궁둥이"
	ENDSELECT
ELSE
	SELECTCASE RAND:3
		CASE 3
			S_SLANGWORD_H += "엉덩이 골짜기"
		CASE 2
			S_SLANGWORD_H += "힙"
		CASE 1
			S_SLANGWORD_H += "엉덩이 살"
		CASEELSE
			S_SLANGWORD_H += "엉덩이"
	ENDSELECT
ENDIF

RETURNF S_SLANGWORD_H


;*************【음어 메모（미등록）】***********************
;빵뎅이
;
;
;
;
;*************【음어 메모（미등록）】***********************



