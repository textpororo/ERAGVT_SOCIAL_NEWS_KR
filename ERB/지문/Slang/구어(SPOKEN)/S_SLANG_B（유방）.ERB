;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「유방」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;
;Nipple(젖꼭지)은 따로 표현하는 것이 있습니다
;유륜이나 젖꼭지는 되도록 표현하지 말고, 지문으로 보충해 주세요
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Bust(バスト)의 음어(淫語)
;**********************************************************
@S_SLANG_B(ARG)
#DIMS S_SLANGWORD_B
#FUNCTIONS

;초기화
S_SLANGWORD_B=




;부위 호칭
IF TALENT:음유 > 0
	IF TALENT:빈유 > 0
		SELECTCASE RAND:3
			CASE 2
				S_SLANGWORD_B += "쾌락에 녹아내리는 작은 가슴"
			CASE 1
				S_SLANGWORD_B += "쾌락에 녹아내르는 음란한 빈유"
			;눌어붙은의 원문은 焼き焦げた
			CASEELSE
				S_SLANGWORD_B += "쾌락에 눌어붙은 찌찌"
		ENDSELECT
	ELSEIF TALENT:거유 > 0
		SELECTCASE RAND:3
			CASE 2
				S_SLANGWORD_B += "쾌락에 녹아내리는 풍만한 가슴"
			;농익은 가슴의 원문은 鞠乳
			CASE 1
				S_SLANGWORD_B += "쾌락이 터져 나오는 음란한 농익은 가슴"
			CASEELSE
				S_SLANGWORD_B += "쾌락에 눌어붙는 빵빵한 가슴"
		ENDSELECT
	ELSE
		SELECTCASE RAND:4
			CASE 3
				S_SLANGWORD_B += "쾌락에 타오르는 가슴"
			CASE 2
				S_SLANGWORD_B += "쾌락에 눌어붙은 바스트"
			CASE 1
				S_SLANGWORD_B += "쾌락에 녹아내리는 음란한 가슴"
			CASEELSE
				S_SLANGWORD_B += "쾌락에 녹아내리는 음란한 찌찌"
		ENDSELECT
	ENDIF
ELSE
	IF TALENT:빈유 > 0
		S_SLANGWORD_B += "찌찌"
	ELSEIF TALENT:거유 > 0
		SELECTCASE RAND:5
			CASE 4
				S_SLANGWORD_B += "젖탱이"
			CASE 3
				S_SLANGWORD_B += "쭈쭈"
			;위의 두 개는 역자가 추가해 보고 싶어서 썼다
			CASE 2
				S_SLANGWORD_B += "바스트"
			CASE 1
				S_SLANGWORD_B += "빵빵한 가슴"
			CASEELSE
				S_SLANGWORD_B += "거유"
		ENDSELECT
	ELSE
		SELECTCASE RAND:4
			CASE 3
				S_SLANGWORD_B += "가슴"
			CASE 2
				S_SLANGWORD_B += "바스트"
			CASE 1
				S_SLANGWORD_B += "젖가슴"
			CASEELSE
				S_SLANGWORD_B += "보유"
		ENDSELECT
	ENDIF
ENDIF

RETURNF S_SLANGWORD_B


;*************【음어 메모（미등록）】***********************
;쌍유(双乳)
;유륜
;젖풍선
;풍유
;유국(乳鞠)
;젖덩어리(乳塊)
;유방
;*************【음어 메모（미등록）】***********************



