;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「질」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;
;질 구멍의 표현이며, Labia(음순)는 @S_SLANG_Labia 에서 표현한다
;Womb(자궁)의 표현도 여기서는 피한다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Vagina(ヴァギナ)의 음어(淫語)
;**********************************************************
@S_SLANG_V(ARG)
#DIMS S_SLANGWORD_V
#FUNCTIONS

;초기화
S_SLANGWORD_V =

;제1형용사：상황（즙에 젖어있는 등등）
SIF (STAIN:3 & 4)
	S_SLANGWORD_V = %S_SLANGWORD_V% %S_SLANG_SEMEN(TARGET)%에 젖어 뚝뚝 떨어지는 

;제2형용사：형태 표현
;제3형용사：동작 표현


;부위 호칭
IF TALENT:음호
	S_SLANGWORD_V += "음호"
ELSEIF TALENT:음란
	SELECTCASE RAND:3
		CASE 2
			S_SLANGWORD_V += "살단지"
		CASE 1
			S_SLANGWORD_V += "암구멍"
		;원문은 ドスケベマンコ
		CASEELSE
			S_SLANGWORD_V += "개변태 보지"
	ENDSELECT
ELSE
	SELECTCASE RAND:4
		CASE 3
			S_SLANGWORD_V += "보지"
		CASE 2
			S_SLANGWORD_V += "여성기"
		CASE 1
			S_SLANGWORD_V += "바기나"
		CASEELSE
			S_SLANGWORD_V += "질"
	ENDSELECT
ENDIF

RETURNF S_SLANGWORD_V

;*************【음어 메모（미등록）】***********************
;육혈(肉穴)
;여자 구멍(女穴)
;육벽(肉壁)
;육통(肉筒)
;질통(膣筒)
;그곳, 거시기?(アソコ)
;비호(秘壷)
;*************【음어 메모（미등록）】***********************



