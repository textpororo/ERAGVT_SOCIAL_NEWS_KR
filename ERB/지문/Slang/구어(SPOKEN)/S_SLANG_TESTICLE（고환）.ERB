;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「고환」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;
;자멘(ザーメン)은 따로 표현되어 있기 때문에, 여기서 잘 다룬다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Testicle(睾丸)의 음어(淫語)
@S_SLANG_Testicle(ARG)
#DIMS S_SLANGWORD_Testicle
#FUNCTIONS

;초기화
S_SLANGWORD_Testicle=

;부위 호칭
IF TALENT:음란 > 0
	SELECTCASE RAND:3
		CASE 2
			S_SLANGWORD_Testicle += "정액 제조기"
		CASE 1
			S_SLANGWORD_Testicle += "정액 뱅크"
		CASEELSE
			S_SLANGWORD_Testicle += "자멘 탱크"
	ENDSELECT
ELSE
	SELECTCASE RAND:4
		;왜 없나 싶어서 따로 추가해 봤다
		CASE 3
			S_SLANGWORD_Testicle += "고환"
		;원문은 玉袋
		CASE 2
			S_SLANGWORD_Testicle += "음낭"
		CASE 1
			S_SLANGWORD_Testicle += "쌍알"
		CASEELSE
			S_SLANGWORD_Testicle += "불알"
	ENDSELECT
ENDIF


;합성하여 완성된 음어를 반환한다
RETURNF S_SLANGWORD_Testicle





;*************【음어 메모（미등록）】***********************
;
;
;
;
;
;*************【음어 메모（미등록）】***********************





