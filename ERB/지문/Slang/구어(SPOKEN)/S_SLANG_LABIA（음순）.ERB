;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「음순」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;
;대음순 %S_SLANG_LabiaMajora(TARGET)% 과
;소음순 %S_SLANG_LabiaMinora(TARGET)% 으로 더욱 세분화되어있다
;
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Labia(陰唇)의 음어(淫語)
@S_SLANG_Labia(ARG)
#DIM LCOUNT
#DIMS S_SLANGWORD_Labia
#FUNCTIONS

;초기화
S_SLANGWORD_Labia =


;1/3 확률로 대음순만, 1/3 확률로 소음순만, 1/3 확률로 대음순・소음순 양쪽을
LCOUNT = RAND:99
IF LCOUNT > 65
	;여성기를 숨기는 피하지방이 많은 부분, 바깥쪽의 균열(ワレメ)을 말한다
	S_SLANGWORD_Labia = %S_SLANGWORD_Labia% %S_SLANG_LabiaMajora(TARGET)%
ELSEIF LCOUNT > 32
	;여성기의 질 구멍을 가리는 부분, 안쪽의 비라비라(ビラビラ)를 말한다
	S_SLANGWORD_Labia = %S_SLANGWORD_Labia% %S_SLANG_LabiaMinora(TARGET)%
ELSE
	;여성기를 숨기는 피하지방이 많은 부분, 바깥쪽의 균열(ワレメ)을 말한다
	S_SLANGWORD_Labia = %S_SLANGWORD_Labia% %S_SLANG_LabiaMajora(TARGET)%

	SELECTCASE RAND:2
		CASE 1
			S_SLANGWORD_Labia += 조사만처리(S_SLANG_LabiaMajora(TARGET),"로") + " 둘러싸인"
			;S_SLANGWORD_Labia += "に囲まれた"
		CASEELSE
			S_SLANGWORD_Labia += "의 안쪽에"
	ENDSELECT

	;여성기의 질 구멍을 가리는 부분, 안쪽의 비라비라(ビラビラ)를 말한다
	S_SLANGWORD_Labia = %S_SLANGWORD_Labia% %S_SLANG_LabiaMinora(TARGET)%
ENDIF


;합성하여 완성된 음어를 반환한다
RETURNF S_SLANGWORD_Labia




