;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「자궁」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;
;Vagina(질)의 더 깊은 곳을 표현하고 싶을 때 이용한다
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/01
;**********************************************************
;Womb(子宮)의 음어(淫語)
;**********************************************************
@S_SLANG_W(ARG)
#DIMS S_SLANGWORD_W
#FUNCTIONS

;초기화
S_SLANGWORD_W =

;제1형용사：상황（즙에 젖어있는 등등）
IF (STAIN:3 & 4)
	SELECTCASE RAND:3
		CASE 2
			S_SLANGWORD_W += "정액으로 가득 채워진"
		CASE 1
			S_SLANGWORD_W += "자멘으로 넘쳐흐르는"
		CASEELSE
			S_SLANGWORD_W += "정액으로 더럽혀진"
	ENDSELECT
	
	;제1형용사 뒤에 띄어쓰기 추가
	S_SLANGWORD_W  +=  " "
ENDIF


;제2형용사：형태 표현
;제3형용사：동작 표현

;부위 호칭
IF TALENT:음호
	S_SLANGWORD_W += "음호"
ELSEIF TALENT:음란
	SELECTCASE RAND:2
		CASE 1
			S_SLANGWORD_W += "숨을 쉬듯 수축하는 살단지"
		CASEELSE
			S_SLANGWORD_W += "빠끔빠끔 입을 벌리는 자궁구"
	ENDSELECT
ELSE
	SELECTCASE RAND:4
		;원문은 赤ちゃんの場所
		CASE 3
			S_SLANGWORD_W += "아기 구멍"
		CASE 2
			S_SLANGWORD_W += "아기집"
		CASE 1
			S_SLANGWORD_W += "아기방"
		CASEELSE
			S_SLANGWORD_W += "자궁"
	ENDSELECT
ENDIF

RETURNF S_SLANGWORD_W


;*************【음어 메모（미등록）】***********************
;
;
;
;
;
;
;
;
;
;*************【음어 메모（미등록）】***********************



