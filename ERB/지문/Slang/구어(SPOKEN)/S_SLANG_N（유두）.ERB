;**********************************************************
;기본 규칙
;가필 수정은 자유롭게 해 주세요
;단, 앞의 분이 쓴 문장은 되도록 지우지 말아 주세요
;
;구술에서의「유두」이므로, 한숨이나 가득 차는(詰まった) 표현 등은 있습니다
;체력이나 발정 등, 스테이터스로 말을 더듬거나 하도록 합시다
;이 변수를 호출할 때에는, 말을 더듬는 것도 포함해서 사용해 주세요
;Bust(가슴)에 대해서는, @S_SLANG_B 를 참고
;
;늘려 주었으면 하는 은어는, 아웃풋하여 리퀘스트를 하는 식으로 합시다
;미등록인 상태로 던져 넣거나, 게시판에 올리거나 하면
;실장될 가능성이 있습니다
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Nipple(ニップル)의 음어(淫語)
;**********************************************************
@S_SLANG_N(ARG)
#DIMS S_SLANGWORD_N
#FUNCTIONS

;초기화
S_SLANGWORD_N =

IF TALENT:음유
	SELECTCASE RAND:3
		;원문은 乳芽 유아 인데, 그대로 쓰면 못 알아볼 것 같아서 바꿨다
		;그런 김에 몇개 추가해 봤는데, 남자일 경우는 생각 안 해봤다 ㅁ?ㄹ
		CASE 2
			S_SLANGWORD_N += "젖가슴 돌기"
		CASE 1
			S_SLANGWORD_N += "음탕한 꼭지"
		CASEELSE
			S_SLANGWORD_N += "젖 손잡이"
	ENDSELECT
ELSE
	SELECTCASE RAND:5
		CASE 4
			S_SLANGWORD_N += "유두"
		CASE 3
			S_SLANGWORD_N += "가슴 끝부분"
		;원문은 乳先
		CASE 2
			S_SLANGWORD_N += "가슴 끝"
		CASE 1
			S_SLANGWORD_N += "젖꼭지"
		CASEELSE
			S_SLANGWORD_N += "니플"
	ENDSELECT
ENDIF

RETURNF S_SLANGWORD_N


;*************【음어 메모（미등록）】***********************
;
;
;*************【음어 메모（미등록）】***********************



