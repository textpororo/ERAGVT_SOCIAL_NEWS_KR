;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は出来る限り消さないでください

;書き込む順番は、クドくなり過ぎないように3要素と部位で表現します
;【実況】汁に濡れている、기생されている、촉수 구속구などを装着など
;【形状】大きさ、深さなど、
;【動作】開閉している、うごめいているなど、動きを表現
;【部位呼称】음란や발정や성격で下品な書き方に
;
;膀胱から尿道と穴までを表現、ペニスなどの表現は@SLANG_Pへ
;남자、후타나리、女で別れるように
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Urethra(尿道)の淫語
;**********************************************************
@SLANG_U(ARG)
#FUNCTIONS
#DIMS SLANGWORD_U

;초기화
SLANGWORD_U=

;第一形容詞：状況（汁など）
IF ISMALE()
	IF TALENT:음핵
		SLANGWORD_U+="精子焼けしている"
	ELSE
		SELECTCASE RAND:4
			CASE 3
				SLANGWORD_U+="尿路"
			CASE 2
				SLANGWORD_U+="小水道"
			CASE 1
				SLANGWORD_U+="おしっこの路"
			CASEELSE
				SLANGWORD_U+="요도"
		ENDSELECT
	ENDIF
ELSEIF TALENT:후타나리 > 0
	IF TALENT:음핵
		SLANGWORD_U+="精液の染み付いた"
	ELSE
		SELECTCASE RAND:5
			CASE 4
				SLANGWORD_U+="방광"
			CASE 3
				SLANGWORD_U+="尿路"
			CASE 2
				SLANGWORD_U+="小水道"
			CASE 1
				SLANGWORD_U+="おしっこの路"
			CASEELSE
				SLANGWORD_U+="요도"
		ENDSELECT
	ENDIF
ELSE
	IF TALENT:음핵
		SLANGWORD_U+="精液の染み付いた"
	ELSE
		SELECTCASE RAND:4
			CASE 3
				SLANGWORD_U+="尿路"
			CASE 2
				SLANGWORD_U+="小水道"
			CASE 1
				SLANGWORD_U+="おしっこの路"
			CASEELSE
				SLANGWORD_U+="요도"
		ENDSELECT
	ENDIF
ENDIF
RETURNF SLANGWORD_U

;*************【淫語覚書（未登録）】***********************
;
;泌尿
;小便
;ピス
;
;
;*************【淫語覚書（未登録）】***********************



