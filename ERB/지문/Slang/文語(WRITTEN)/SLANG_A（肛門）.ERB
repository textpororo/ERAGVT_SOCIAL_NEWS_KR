;**********************************************************
;基本ルール
;加筆修正は、自由に行ってください
;ただし、前の方が書いた文章は成るべく消さないでください

;書き込む順番は、クドくなり過ぎないように3要素と部位で表現します
;【実況】汁に濡れている、기생されている、촉수 구속구などを装着など
;【形状】大きさ、深さなど、
;【動作】開閉している、うごめいているなど、動きを表現
;【部位呼称】음란や발정や성격で下品な表現を
;
;尻穴を中心に表現することを努める、尻肉は@SLANG_Hへ
;プレイ内容では、挿入しないかもしれないので、
;内部の直腸や結腸などの表現は控えましょう
;腸液など分泌液などは、%SLANG_Juice(TARGET,"장액")%で用意しています
;
;増やして欲しい隠語は、アウトプットしてリクエストなりしましょう
;未登録の中に放り込むなり、掲示板にアップするなりすれば、
;実装される可能性があります
;
;by 雑魚の人 2018/02/11
;**********************************************************
;Anal(애널)の淫語
;**********************************************************
@SLANG_A(ARG)
#DIMS SLANGWORD_A
#FUNCTIONS

;초기화
SLANGWORD_A=


;**********************************************************
;第一形容詞：状況（汁など）
;**********************************************************
;戦闘状況の場合
;*************************
;끈적끈적にされた
IF (TCVARn:12 & 끈적끈적)
	SLANGWORD_A+="ベトベトにされている"
ENDIF
;ぶっかけられた
IF (STAIN:4 & 4)
	SLANGWORD_A = %SLANGWORD_A%%SLANG_SEMEN(TARGET)%の滴り落ちている
ENDIF



;*************************
;PALAM:윤활の値に依存
;*************************

;どの様な液体が
IF PALAM:윤활 >= 2000 && PALAM:쾌Ａ> 2000
	SLANGWORD_A = %SLANGWORD_A%%SLANG_Juice(TARGET,"직장액")%
	SELECTCASE RAND:3
		CASE 2
			SLANGWORD_A += "をダムの放流の如く出している"
		CASE 1
			SLANGWORD_A += "がせき止められず広がっている"
		CASEELSE
			SLANGWORD_A += "が止め処なく溢れている"
	ENDSELECT


ELSEIF PALAM:윤활 < 2000 && PALAM:윤활 >= 1000 && PALAM:쾌Ａ> 1000
	SLANGWORD_A = %SLANGWORD_A%%SLANG_Juice(TARGET,"직장액")%
	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_A += "を垂らしている"
		CASEELSE
			SLANGWORD_A += "が漏れている"
	ENDSELECT


ELSEIF PALAM:윤활 < 1000 && PALAM:윤활 >= 200 && PALAM:쾌Ａ> 200
	SLANGWORD_A = %SLANGWORD_A%%SLANG_Juice(TARGET,"직장액")%

	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_A += "が滲む"
		CASEELSE
			SLANGWORD_A += "が染みている"
	ENDSELECT

ELSE
	SELECTCASE RAND:2
		CASE 1
			SLANGWORD_A += "ほとんど汁気の無い"
		CASEELSE
			SLANGWORD_A += "ろくに準備されてい無い"
	ENDSELECT
ENDIF



;第二形容詞：形状表現


;第三形容詞：動作表現


;部位呼称
IF TALENT:음고>0 || TALENT:음란>0
	SELECTCASE RAND:4
		CASE 3
			SLANGWORD_A += "尻オナホ"
		CASE 2
			SLANGWORD_A += "애널"
		CASEELSE
			;尻マンコ
			SELECTCASE RAND:2
				CASE 1
					SLANGWORD_A += "尻"
				CASEELSE
					SLANGWORD_A += "ケツ"
			ENDSELECT
			SLANGWORD_A += "マンコ"
	ENDSELECT
ELSE
	SELECTCASE RAND:5
		CASE 4
			SLANGWORD_A += "排泄器官"
		CASE 3
			SLANGWORD_A += "애널"
		CASE 2
			SLANGWORD_A += "엉덩이"
		CASE 1
			SLANGWORD_A += "菊"
			SELECTCASE RAND:3
				CASE 2
					SLANGWORD_A += "花"
				CASE 1
					SLANGWORD_A += "門"
				CASEELSE
					SLANGWORD_A += "穴"
			ENDSELECT
		CASEELSE
			SELECTCASE RAND:3
				CASE 2
					SLANGWORD_A += "不浄"
				CASE 1
					SLANGWORD_A += "後ろ"
				CASEELSE
					SLANGWORD_A += "엉덩이"
			ENDSELECT
			SLANGWORD_A += "の穴"
	ENDSELECT
ENDIF

RETURNF SLANGWORD_A


;*************【淫語覚書（未登録）】***********************
;애널ローズ
;肛悦
;
;
;
;*************【淫語覚書（未登録）】***********************
;
;*************【淫語覚書（別に用意する予定）】***********************
;S字結腸
;腸粘膜
;腸壁
;腸道
;*************【淫語覚書（別に用意する予定）】***********************


