
;전투 지원
@SUPPORT
    CFLAG:101 = -1

	PRINTL 
	PRINTFORML %타겟은% 오퍼레이션 업무에 전념하고 있다……
	LOCAL = MAXBASE:체력
	IF FLAG:43 == 1
		TIMES LOCAL, 0.24
	ELSEIF FLAG:43 == 2
		TIMES LOCAL, 0.18
	ENDIF
	;헌신적이라면 소비가 증가한다
	SIF TALENT:헌신적 > 0
		TIMES LOCAL, 1.10
	BASE:체력 = LIMIT(BASE:체력 - SYOUHI_KEIGEN(TARGET,LOCAL,체력), 0 , BASE:체력)

	LOCAL = MAXBASE:기력
	IF FLAG:43 == 1
		TIMES LOCAL, 0.24
	ELSEIF FLAG:43 == 2
		TIMES LOCAL, 0.18
	ENDIF
	;헌신적이라면 소비가 증가한다
	SIF TALENT:헌신적 > 0
		TIMES LOCAL, 1.10
	BASE:기력 = LIMIT(BASE:기력 - SYOUHI_KEIGEN(TARGET,LOCAL,기력), 0 , BASE:기력)

	CALL GET_SYUREN, (15 + RAND:11)
	CALL TENTACLE_LEVEL
	LOCAL = (RESULT - 3)/ABL:레벨 * 10 + RAND:5
	CALL GET_EXP,min(LOCAL,150)

	IF RAND:3 == 0
		BASE:지성 += 2
		PRINTFORML 지성 기초치가 2 올랐다.
	ELSEIF RAND:3 < 2
		BASE:지성 += 1
		PRINTFORML 지성 기초치가 1 올랐다.
	ENDIF

	IF CONFIG_CHECK_SCREEN_F(3) == 0
		PRINTW 
	ELSE
		PRINTL 
	ENDIF
