
;戦闘支援
@SUPPORT
    CFLAG:101 = -1

	PRINTL 
	PRINTFORML %CALLNAME%はオペレーション業務に専念している……
	LOCAL = MAXBASE:체력
	IF FLAG:43 == 1
		TIMES LOCAL, 0.24
	ELSEIF FLAG:43 == 2
		TIMES LOCAL, 0.18
	ENDIF
	;헌신적だと消費が増加
	SIF TALENT:헌신적 > 0
		TIMES LOCAL, 1.10
	BASE:체력 = LIMIT(BASE:체력 - SYOUHI_KEIGEN(TARGET,LOCAL,체력), 0 , BASE:체력)

	LOCAL = MAXBASE:기력
	IF FLAG:43 == 1
		TIMES LOCAL, 0.24
	ELSEIF FLAG:43 == 2
		TIMES LOCAL, 0.18
	ENDIF
	;헌신적だと消費が増加
	SIF TALENT:헌신적 > 0
		TIMES LOCAL, 1.10
	BASE:기력 = LIMIT(BASE:기력 - SYOUHI_KEIGEN(TARGET,LOCAL,기력), 0 , BASE:기력)

	CALL GET_SYUREN, (15 + RAND:11)
	CALL TENTACLE_LEVEL
	LOCAL = (RESULT - 3)/ABL:레벨 * 10 + RAND:5
	CALL GET_EXP,min(LOCAL,150)

	IF RAND:3 == 0
		BASE:지성 += 2
		PRINTFORML 지성の基礎値が2上がった
	ELSEIF RAND:3 < 2
		BASE:지성 += 1
		PRINTFORML 지성の基礎値が1上がった
	ENDIF

	IF CONFIG_CHECK_SCREEN_F(3) == 0
		PRINTW 
	ELSE
		PRINTL 
	ENDIF
